/* jQuery(document).ready(function ($) {
	$("#hs-my-list").click(function() {
		$("#my-list-icos-expandable").toggleClass('is-visible');
		if ($("#hs-trigger p").text() == "Hide Starred ICO's") {
				$("#hs-trigger p").text("Show Starred ICO's");
		} else {
			$("#hs-trigger p").text("Hide Starred ICO's");
		};
	});
}); */
